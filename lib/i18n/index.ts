// Default language: Serbian (Latin)
import { sr } from './sr'

// Export the default language as `t`
// This structure allows adding more languages later without changing components
export const t = sr

// Type export for better TypeScript support
export type Translations = typeof sr

